{
  "name": "cloud-realify",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "verify": "node scripts/verify-release.mjs"
  },
  "engines": { "node": ">=20" }
}
